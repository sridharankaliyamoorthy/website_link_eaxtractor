---
alwaysApply: true
---

# Git Commit Message Guidelines

Follow conventional commit format:
- `feat:` for new features
- `fix:` for bug fixes
- `docs:` for documentation changes
- `style:` for formatting changes
- `refactor:` for code refactoring
- `test:` for adding/updating tests
- `chore:` for maintenance tasks
- `perf:` for performance improvements
- `ci:` for CI/CD changes
- `build:` for build system changes

Format: `type(scope): description`

Examples:
- `feat(api): add user authentication endpoint`
- `fix(ui): resolve button alignment issue on mobile`
- `docs: update installation instructions`
- `refactor: improve code organization`
- `test: add unit tests for link extraction`

Body (optional):
- Explain the "what" and "why" vs. "how"
- Wrap at 72 characters
- Reference issues: `Fixes #123`, `Closes #456`

Footer (optional):
- Breaking changes: `BREAKING CHANGE: description`

## 🚀 Auto-Push After Commit

When you mention `@gitcommit.mdc`, the system will help you commit and push automatically.

### Smart Repository Detection

The system checks for repository URL in this order (avoids duplicate prompts):
1. ✅ **Git remote** (highest priority)
2. ✅ **Config file** (`.git-repo-config`)
3. ✅ **Environment variable** (`GIT_REPO_URL`)
4. ✅ **`.env` file**

**Only prompts if none are found** - ensuring a clean, one-time setup experience.

### Automatic Options:

**Option 1: Git Hook (Automatic)**
After every `git commit`, the system will:
- ✅ Check for existing repository URL (no duplicate prompts!)
- ✅ Only ask if repository URL is not found
- ✅ Save URL for future use
- ✅ Prompt to push automatically

**Option 2: Auto-Push Script**
```bash
./scripts/auto-push.sh "your commit message"
```
This will:
- Stage all changes
- Commit with your message
- Check for existing repository URL (smart detection)
- Only prompt if not found (one-time setup)
- Save URL to config file for future use
- Push to remote automatically

**Option 3: Check Repository Status**
```bash
source scripts/git-utils.sh && show_repo_status
```
Shows current repository configuration and where it's stored.

### Repository Configuration

The repository URL is automatically saved to:
- `.git-repo-config` file (for persistence)
- Git remote (for git commands)
- Environment variable (optional)

**Benefits:**
- ✅ No repeated prompts
- ✅ One-time setup
- ✅ Works across sessions
- ✅ Efficient workflow
